interface Items {
  name?: string;
  type?: string;
  prices?: number;
  priceFor?: string;
}

const items:Items = {
    name: "oval hat",
    type: "hats",
    pricesFor: "regular",
    prices:30000
}